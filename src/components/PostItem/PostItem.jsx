export  default function PostItem({post}) {

    return (
        <div>
            <h1>Title: {post.title}</h1>
            <p>Text: {post.text}</p>
            <form>
                
            </form>
        </div>
    )
}